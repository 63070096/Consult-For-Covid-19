ขั้นตอนการเปิดใช้งาน Application

1. ทำการเปิดตัว Application ที่ได้ทำการ Compile แล้ว เป็นไฟล์ .exe
2. เมื่อเข้ามาใน App จะปรากฎหน้า Main Menu ขึ้นมา หากผู้ใช้ต้องการจะตรวจสอบว่าผู้ใช้ได้รับเชื้อแล้วหรือยัง
   ให้ทำการเลือก Start Survey
3. ทำการกรอกประวัติส่วนตัวโดยคร่าวๆ
4. ให้ผู้ใช้ทำการตอบคำถามไปเรื่อย
5. เมื่อผู้ใช้ทำการตอบคำถามเสร็จระบบจะทำการประมวลผลแล้วบอกสถานะของผู้ใช้
